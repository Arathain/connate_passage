package arathain.miku_machines.config;

import eu.midnightdust.lib.config.MidnightConfig;

public class ConnateConfig extends MidnightConfig {

	// Whether or not the spline carriage's spline should be rendered
	@Entry
	public static boolean renderCarriage = true;
}
